language: python
python:
  - "2.7"
  - "3.6"

install:
  - pip install -r requirements.txt pyyaml codecov coverage

script:
  - coverage run --source=marshmallow_objects setup.py test

after_success:
  - codecov
